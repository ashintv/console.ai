{
  "name": "console-ai",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"pnpm dev:api\" \"pnpm dev:webv2\"",
    "dev:api": "pnpm --filter @console-ai/api dev",
    "dev:webv2": "PORT=3001 pnpm --filter webv2 dev",
    "dev:example": "pnpm --filter @console-ai/example dev",
    "build": "pnpm --filter \"./apps/**\" --filter \"./packages/**\" run build",
    "build:api": "pnpm --filter @console-ai/api build",
    "build:webv2": "pnpm --filter webv2 build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.32.1",
  "dependencies": {
    "highlight.js": "^11.11.1"
  }
}
